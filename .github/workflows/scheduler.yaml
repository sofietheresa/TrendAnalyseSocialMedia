name: Trigger Scraper Every 15 Minutes

on:
  schedule:
    - cron: "*/5 * * * *" # Alle 15 Minuten (UTC)
  workflow_dispatch: # Manuell triggerbar über GitHub UI

jobs:
  run-scraper:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger /run-scrapers Endpoint on Render
        env:
          API_SECRET: ${{ secrets.API_SECRET }}
        run: |
          curl -X POST https://trendanalysesocialmedia.onrender.com/run-scrapers \
            -H "Authorization: Bearer $API_SECRET"
